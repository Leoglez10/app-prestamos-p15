# SDD Init — app-prestamos-p15

**Detected**: 2026-07-14
**Persistence**: openspec
**Strict TDD**: false (no test framework installed)

## Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Frontend | React | 19.1.0 |
| Router | react-router-dom | 7.13.1 |
| Build | Vite | 7.0.4 |
| Language | TypeScript | 5.8.3 |
| Desktop | Tauri | 2.x (CLI 2.11.2) |
| Backend | Rust | 2021 edition |
| DB | SQLite via @tauri-apps/plugin-sql | 2.4.0 |
| Persistence | serde + serde_json | 1.x |

## Architecture

- Single-window Tauri desktop app, identifier `com.p15.prestamos`
- 4 Rust commands: `get_database_url`, `create_backup`, `list_backups`, `restore_backup_from_bytes`
- React frontend with data layer in `src/hooks/useInventory.ts`
- Admin auth in `Admin.tsx` using `loginAdmin(codigo, pin)`, session in `window.sessionStorage["p15_admin_session"]`
- `PrestamoRapido.tsx` is unauthenticated, writes to `prestamos_rapidos_alumnos`

## Testing Capabilities

**Runner**: none
**test_command**: none
**CI**: none detected

| Layer | Available |
|-------|-----------|
| Unit | ❌ |
| Integration | ❌ |
| E2E | ❌ |
| Coverage | ❌ |
| Type checker | ✅ (`npx tsc --noEmit`) |

## Conventions

- Vite config at root
- Tauri config in `src-tauri/`
- React components in `src/`
- Hooks/data layer in `src/hooks/`
- No test files, no test config, no CI workflows

## Notes for Upcoming Change

- `admin-auth-prestamo-rapido`: auth gate on Préstamo Rápido
- Consider AuthContext pattern + persistence + auto-lock
- Dead `admin_users` table exists in schema but is never used
- `-LeoLaptop` file duplicates carry drift — avoid touching them
- Default admin creds hardcoded: code `223992647`, PIN `#admin*p15#`
